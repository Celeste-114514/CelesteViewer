<?xml version="1.0" encoding="utf-8"?>
<!--
https://go.microsoft.com/fwlink/?LinkID=208121.

===== 2026-09-14 修正（发布后启动闪退的真凶）=====

原来这里是 MSIX 模板生成的默认值：

    <PublishDir>bin\$(Configuration)\$(TargetFramework)\$(RuntimeIdentifier)\publish\</PublishDir>

问题：本项目是 WinUI3 应用，XamlCompiler 生成 .xbf / .pri 时走的路径是带平台层的
（obj\x64\Release\...  →  bin\x64\Release\...），而上面这个 PublishDir 不含 $(Platform)，
两边对不上，发布时整批 XAML 资源复制不过去。

后果非常隐蔽：publish 命令**不报任何错**、产物看起来也齐全（128MB / 57 个文件），
但发布目录里一个 .xbf、一个 .pri 都没有。装到机器上一启动就死在
MainWindow 的 LoadComponent，日志只有一句 no such file / 0x80004005。
（2026-09-14 实测：Debug build 输出有 8 个 xbf + pri，能跑；Release publish 输出 0 个，必崩。）

修法：PublishDir 补上 $(Platform)，跟 build 的 OutDir 体系保持一致，
csc/XamlCompiler/publish 三方路径统一，xbf 和 pri 才会正常落到发布目录。

===== 另外两处按项目现状调整 =====

SelfContained：改成 false（框架依赖），跟发布策略一致 ——
用户机器上装 .NET 9 运行时 + Windows App SDK 2.x 运行时即可，安装包只有 35MB；
缺运行组件由安装程序检测并提示。将来若要发自包含版本，
命令行加 -p:SelfContained=true 即可覆盖这里，不必改文件。
（注意：PublishTrimmed 跟着 SelfContained 自动走，见 CelesteGallery.csproj 里的说明。）

RuntimeIdentifier：保持 win-x64。主项目 csproj 里"刻意不设 RuntimeIdentifier"那段注释
说的是**编译期**（免得 XamlCompiler 多一层目录抽风）；发布期必须有具体 RID 才能出框架依赖产物。
-->
<Project ToolsVersion="4.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup>
    <PublishProtocol>FileSystem</PublishProtocol>
    <Platform>x64</Platform>
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
    <!-- 关键：带 $(Platform) 一层，与 XamlCompiler 的 obj/输出路径体系对齐 -->
    <PublishDir>bin\$(Platform)\$(Configuration)\$(TargetFramework)\$(RuntimeIdentifier)\publish\</PublishDir>
    <SelfContained>false</SelfContained>
    <PublishSingleFile>False</PublishSingleFile>
  </PropertyGroup>
</Project>
